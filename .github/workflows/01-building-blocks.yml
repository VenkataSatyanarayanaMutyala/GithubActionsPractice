name: Building Blocks

on:
 workflow_dispatch:

jobs:
  echo-hello:
   runs-on: ubuntu-latest
   steps:
    - run: echo "Say Hello"
  echo-goodbye:
   runs-on: ubuntu-latest
   steps:
    - run: echo "exit with a zero code"
    - run: echo "Say goodbye"
   
